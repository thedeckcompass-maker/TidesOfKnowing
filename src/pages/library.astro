---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { siteBase } from "../lib/site";
import { sortBlogPostsForListing } from "../lib/sortBlogPosts";

const posts = sortBlogPostsForListing(
  await getCollection("blog", ({ data }) => !data.draft),
);

const ogUrl = new URL("/library/", siteBase(Astro)).href;
---

<BaseLayout
  title="Blog — Tides of Knowing | Journal & announcements"
  description="Blog posts and journal entries from Tides of Knowing. For methodology articles, series, and topics, browse the article library."
  ogUrl={ogUrl}
>
  <div class="library page-inner">
    <header class="library__head" data-reveal>
      <h1 class="library__title">Blog</h1>
      <p class="library__lede">
        This page is the blog roll — journal-style posts and announcements. The main long-form
        catalog (featured articles, series, topics, and pagination) lives in the{" "}
        <a href="/articles/">article library</a>.
      </p>
    </header>

    {
      posts.length === 0 ? (
        <p class="library__empty" data-reveal>
          The first blog posts are on their way. For methodology articles, visit the{" "}
          <a href="/articles/">article library</a>. Until then, learn more{" "}
          <a href="/about">about this publication</a> or join the{" "}
          <a href="/subscribe/">Join the list</a> for updates.
        </p>
      ) : (
        <ul class="library__list">
          {posts.map((post) => (
            <li class="library__item" data-reveal>
              <article>
                <a class="library__link" href={`/blog/${post.slug ?? post.id}/`}>
                  <h2 class="library__post-title">{post.data.title}</h2>
                  <time
                    class="library__date"
                    datetime={post.data.date.toISOString()}
                  >
                    {post.data.date.toLocaleDateString("en-AU", {
                      year: "numeric",
                      month: "long",
                      day: "numeric",
                    })}
                  </time>
                  <p class="library__desc">{post.data.description}</p>
                </a>
              </article>
            </li>
          ))}
        </ul>
      )
    }
  </div>
</BaseLayout>

<style>
  .library {
    padding: clamp(2rem, 6vw, 3.5rem) 0 clamp(3rem, 8vw, 5rem);
    max-width: 40rem;
  }

  .library__head {
    margin-bottom: 2.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--gold);
  }

  .library__title {
    margin: 0 0 0.75rem;
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(2.25rem, 5vw, 2.85rem);
    color: var(--mountain-water);
  }

  .library__lede {
    margin: 0;
    font-size: 1.08rem;
    line-height: 1.75;
  }

  .library__empty {
    margin: 0;
    font-size: 1.05rem;
    line-height: 1.75;
  }

  .library__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .library__link {
    display: block;
    text-decoration: none;
    color: inherit;
    padding: 0.25rem 0;
    border-bottom: 1px solid rgba(198, 161, 74, 0.35);
    transition: border-color 0.2s ease;
  }

  .library__link:hover {
    border-bottom-color: var(--sky);
  }

  .library__link:hover .library__post-title {
    color: var(--sky);
  }

  .library__post-title {
    margin: 0 0 0.4rem;
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: 1.5rem;
    color: var(--mountain-water);
    transition: color 0.2s ease;
  }

  .library__date {
    display: block;
    font-family: var(--font-nav);
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 0.65rem;
  }

  .library__desc {
    margin: 0;
    font-size: 1rem;
    line-height: 1.7;
    opacity: 0.9;
  }
</style>
