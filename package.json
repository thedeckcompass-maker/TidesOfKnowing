{
  "name": "tides-of-knowing",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "publish-article": "node scripts/add-article.mjs",
    "astro": "astro",
    "publish": "npm run build && wrangler pages deploy dist --project-name=tidesofknowing"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
